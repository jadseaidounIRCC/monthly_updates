# Database Configuration
# =======================

# Database Type Selection (optional)
# Options: 'mysql', 'sqlite', or leave unset for auto-detection
# DATABASE_TYPE=sqlite

# Force specific database (optional)
# USE_SQLITE=true    # Forces SQLite even if MySQL is available
# USE_MYSQL=true     # Forces MySQL (will fail if not available)

# SQLite Configuration
# SQLITE_DB_PATH=./data/monthly_updates.db  # Optional, defaults to ./data/monthly_updates.db

# MySQL Configuration (for production/Azure)
DB_HOSTNAME=your-database.mysql.database.azure.com
DB_PORT=3306
DB_NAME=monthly_updates
DB_USER=your_db_user
DB_PASS=your_db_password
DB_SSL_MODE=require
DB_SSL_CERT=ssl_cert
DB_USE_LOCAL=False

# Application Configuration
NODE_ENV=development
PORT=3001
RUN_LOCAL=True
JWT_SECRET=your_very_secure_jwt_secret_here_change_in_production
CORS_ORIGIN=http://localhost:3000

# Logging Configuration
LOG_LEVEL=debug
LOG_FILE=./logs/app.log

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security
BCRYPT_ROUNDS=12
SESSION_SECRET=your_session_secret_here